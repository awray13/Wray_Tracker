@using Wray_Tracker.Models
@model Ticket


<!-- Content Wrapper. Contains page content -->

    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Ticket Dashboard</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Tickets")">Home</a></li>
                        <li class="breadcrumb-item active">Ticket Dashboard</li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">

        <!-- Default box -->
        <div class="card">
            <div class="card-header">
                <h1 class="card-title text-primary"><i class="fas fa-ticket-alt"></i> @Html.DisplayFor(model => model.Title)</h1>

                <div class="card-tools">
                    <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                        <i class="fas fa-minus"></i>
                    </button>
                    <button type="button" class="btn btn-tool" data-card-widget="remove" data-toggle="tooltip" title="Remove">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-12 col-md-12 col-lg-8 order-2 order-md-1">
                        <div class="row">
                            <div class="col-12 col-sm-4">
                                <div class="info-box bg-cyan">
                                    <div class="info-box-content">
                                        <span class="info-box-number text-center text-light">Developer</span>
                                        <span class="info-box-text text-center text-light mb-0">@Model.Developer.FullName</span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12 col-sm-4">
                                <div class="info-box bg-light">
                                    <div class="info-box-content">
                                        <span class="info-box-text text-center text-muted">Total amount spent</span>
                                        <span class="info-box-number text-center text-muted mb-0">2000</span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12 col-sm-4">
                                <div class="info-box bg-light">
                                    <div class="info-box-content">
                                        <span class="info-box-text text-center text-muted">Estimated project duration</span>
                                        <span class="info-box-number text-center text-muted mb-0">20 </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <h4>Comments</h4>
                                @foreach (var comment in Model.Comments)
                                {
                                    <div class="post">
                                        <div class="user-block">
                                            <img class="img-circle img-bordered-sm" src="~/Images/user.svg" alt="user image">
                                            <span class="username">
                                                @comment.User.FullName
                                            </span>
                                            <span class="description">
                                                @comment.Created.ToString("MMM dd, yyyy")
                                            </span>
                                        </div>

                                        <!-- /.user-block -->
                                        <div>
                                            <p>
                                                @Html.Raw(comment.Body)
                                            </p>
                                        </div>
                                    </div>
                                }
                            </div>
                            <div class="col-12">
                                @if (User.Identity.IsAuthenticated)
                                {
                                    using (Html.BeginForm("Create", "TicketComments", FormMethod.Post, null))
                                    {
                                        @Html.AntiForgeryToken()
                                        @Html.Hidden("TicketId", Model.Id)
                                        @Html.HiddenFor(model => model.Id)

                                        @Html.TextArea("CommentBody")
                                        <br />
                                        <button type="submit" class="btn btn-secondary">Add Comment</button>
                                    }
                                }
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-12 col-lg-4 order-1 order-md-2">
                        <h4 class="text-primary"><i class="fas fa-user-alt"></i> @Html.DisplayFor(model => model.Submitter.FullName)</h4>
                        <p class="text-muted">@Html.DisplayFor(model => model.Description)</p>
                        <br>
                        <div class="text-muted">
                            <p class="text-sm">
                                Ticket Creation Date
                                <b class="d-block">@Model.Created.ToString("MMM dd, yyyy")</b>
                            </p>
                        </div>

                        <h5 class="mt-5 text-muted">Attachments</h5>
                        @using (Html.BeginForm("Create", "TicketAttachments", FormMethod.Post, new { enctype = "multipart/form-data" }))
                        {
                            @Html.AntiForgeryToken()
                            @Html.Hidden("TicketId", Model.Id)

                            <ul class="list-unstyled">
                                <li>
                                    <a href="" class="btn-link text-secondary"><i class="far fa-fw fa-file-word"></i> Functional-requirements.docx</a>
                                </li>
                                <li>
                                    <a href="" class="btn-link text-secondary"><i class="far fa-fw fa-file-pdf"></i> UAT.pdf</a>
                                </li>
                                <li>
                                    <a href="" class="btn-link text-secondary"><i class="far fa-fw fa-envelope"></i> Email-from-flatbal.mln</a>
                                </li>
                                <li>
                                    <a href="" class="btn-link text-secondary"><i class="far fa-fw fa-image "></i> Logo.png</a>
                                </li>
                                <li>
                                    <a href="" class="btn-link text-secondary"><i class="far fa-fw fa-file-word"></i> Contract-10_12_2014.docx</a>
                                </li>
                            </ul>
                            <div class="text-center mt-5 mb-3">
                                <button type="submit" class="btn btn-sm btn-secondary">Add Files</button><br />
                                <input type="file" class="btn btn-default" name="newAttachment" />
                            </div>
                        }
                    </div>
                </div>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->

    </section>
    <!-- /.content -->

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>